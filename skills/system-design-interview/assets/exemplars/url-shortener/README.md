# URL Shortener — HLD Exemplar (partial)

This folder contains **only** `06-high-level-design.md` — a **generic A1 CRUD**
exemplar for Phase 4 calibration.

Use when the prompt is: point lookup, redirect, key-value CRUD, read-heavy API.

**Do not use** for streaming, feed, or top-K problems — see `trending-articles-top-k`.
